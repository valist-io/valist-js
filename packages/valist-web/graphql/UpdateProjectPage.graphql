query UpdateProjectPage($projectId: String!){
  project(id: $projectId){
    id
    metaURI
    account {
      members {
        id
      }
    }
    releases(orderBy: blockTime, orderDirection: "desc") {
      id
      name
      metaURI
    }
    members {
      id
    }
  }
}