{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "test": "lerna run test",
    "lint": "lerna run lint",
    "build": "lerna run build",
    "dev": "lerna run deploy:ganache && lerna run bootstrap && lerna run dev --parallel"
  }
}
